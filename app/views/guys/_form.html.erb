<%= form_for(@guy) do |f| %>
  <% if @guy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guy.errors.count, "error") %> prohibited this guy from being saved:</h2>

      <ul>
      <% @guy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :person %><br>
    <%= f.check_box :person %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
